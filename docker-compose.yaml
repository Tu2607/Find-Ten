services:
  find-ten-game:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - find-ten-data:/app/data
    restart: unless-stopped

volumes:
  find-ten-data:
